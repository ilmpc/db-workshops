{
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "mhutchie.git-graph",
        "mongodb.mongodb-vscode"
      ]
    }
  },
  "postCreateCommand": "docker run -p27017:27017 -d -v '$(pwd)/restaurants.json':/app/rest.json --name db mongo && docker exec db mongoimport /app/rest.json"
}